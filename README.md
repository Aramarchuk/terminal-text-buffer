# Terminal Text Buffer

A Kotlin implementation of a terminal text buffer with support for cursor operations, text editing, and content access.

## Features

- Configurable window size and scrollback
- Cursor navigation and positioning
- Text editing with attribute support
- Content access from screen and scrollback
- Line-based operations

## Building

```bash
./gradlew build
```

## Running

```bash
./gradlew run
```

Tell em about numeration of buffer.
Explain your decision about removing setSize functions.
Splitting buffer to ScreenContent and Scrollback

